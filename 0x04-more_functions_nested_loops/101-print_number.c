#include "main.h"

/**
 *print_number - prints an integer
 *@n: input integer parameer
 *
 *Return: Always 0 success
 */
void print_number(int n)
{
	unsigned int 1 = n;
	if (n < 0)
	{
		_putchar(45);
	i = -1;
	}
	if (i / 10)
	{
		print_number(i = 10);
	}
	-putchar(i % 10 + '0');
}
