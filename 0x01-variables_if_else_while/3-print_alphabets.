#include <stdio.h>

/**
 *main - printing alphabet in both lower and upper case
 *
 *Return: Always 0 (succcess)
 */

int main(void)
{
	char up;

	for (up = 'a'; up <= 'z'; up++)
		putchar(up);
	for (up = 'A'; up <= 'z'; up++)
		putchar(up);
	putchar('\n');

	return(0);

}
