#include <stdio.h>

/**
 *main - computes and prints the sum of all the multiples of
 *3 or 5 below 1024
 *Return: Nothing
 */

int main(void)
{
	int k, res;

	for (k = 0, k < 1024, k++)
		if (k % 3 == 0 || k % 5 == 0)
			res += i;
		printf("%d\n", res);
		return (0);
}
